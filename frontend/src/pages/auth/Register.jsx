import { Container, Typography } from "@mui/material";

export default function Register() {
  return (
    <Container sx={{ mt: 10 }}>
      <Typography variant="h3">
        Register
      </Typography>
    </Container>
  );
}